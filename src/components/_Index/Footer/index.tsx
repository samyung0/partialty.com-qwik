import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <section class="h-[70vh] bg-primary-dark-gray">
      <div class="w=[900px] flex  ">
        <div></div>
      </div>
    </section>
  );
});
