import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <nav>
      <ul>
        <li>Courses</li>
        <li>Playground</li>
        <li>Pricing</li>
        <li>Login</li>
      </ul>
    </nav>
  );
});
