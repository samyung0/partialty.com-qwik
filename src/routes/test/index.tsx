import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <>
      {/* <QwikAtomDarkCM
        template="nextjs"
        dependencies={{
          react: "^18",
          "react-dom": "^18",
          next: "14.1.0",
        }}
      /> */}
    </>
  );
});
